[Socket]
# Reset the vendor's wildcard listener, then bind the opt-in management surface
# only to the local host. Remote publication requires a separate, deliberate
# Tailscale Serve or firewall decision.
ListenStream=
ListenStream=127.0.0.1:9090
ListenStream=[::1]:9090
