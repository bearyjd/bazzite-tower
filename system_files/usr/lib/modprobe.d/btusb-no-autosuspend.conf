# Bluetooth: disable USB autosuspend on the controller.
#
# btusb autosuspend drops the link on this ThinkPad's Intel controller — paired
# devices go silent until something re-wakes it. Costs a little idle power.
# (bluetooth.disable_ertm=1 is set separately as a karg.)
options btusb enable_autosuspend=0
